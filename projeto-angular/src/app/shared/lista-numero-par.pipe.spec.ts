import { ListaNumeroParPipe } from './lista-numero-par.pipe';

describe('ListaNumeroParPipe', () => {
  it('create an instance', () => {
    const pipe = new ListaNumeroParPipe();
    expect(pipe).toBeTruthy();
  });
});
